<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.julia.myproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Calc"
            android:label="@string/title_activity_calc"
            android:theme="@android:style/Theme.Black" />
        <activity
            android:name=".GraphView1"
            android:label="@string/title_activity_graph_view1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GraphView2"
            android:label="@string/title_activity_graph_view2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Aboutapp"
            android:label="@string/title_activity_aboutapp"></activity>
    </application>

</manifest>